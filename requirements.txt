playwright>=1.44.0
python-dotenv>=1.0.0

# Web API layer (Round 2+)
fastapi>=0.111.0
uvicorn[standard]>=0.30.0
pydantic>=2.7.0

# LLM integration (recommended for natural language command parsing)
# Install with: pip install litellm
# Then set LITELLM_MODEL in your .env, e.g.:
#   LITELLM_MODEL=ollama/llama3.2          # for local Ollama
#   LITELLM_MODEL=gpt-4o-mini              # or any supported model
#   LITELLM_MODEL=xai/grok-2               # for xAI Grok (requires XAI_API_KEY)
# litellm>=1.40.0

# Optional helpers
# rich>=13.0.0   # for prettier CLI output (future)
